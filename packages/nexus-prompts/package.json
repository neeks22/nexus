{
  "name": "nexus-prompts",
  "version": "0.1.0",
  "description": "Three-layer prompt architecture for dealership AI agents — safety rails, tenant config, client customization",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "npx tsup src/index.ts --format esm --dts",
    "test": "npx vitest run"
  },
  "keywords": ["prompts", "dealership", "automotive", "ai", "safety-rails", "multi-tenant"],
  "license": "MIT",
  "dependencies": {
    "zod": "^4.3.6"
  }
}
