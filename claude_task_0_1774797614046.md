Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Created at `/Users/sayah/nexus/docs/agency/nesb-copywriting-framework.md`.

The doc includes everything you asked for:

- **Full NESB breakdown** with emotional subtext for each element — why it works, not just what it is
- **Not Statements** section with the top 8 subprime objections and an example block ready to copy
- **4 Sections of copy** (Headline, Lead, Body, Offer) with formulas for each
- **Dealership application** split by segment: subprime, near-prime, service, retargeting
- **NESB scoring rubric** (1–5 each, 16+ = ship it) with audit questions
- **10 before/afters** with actual scores on both versions — so the team can see exactly why the weak copy fails and which element the rewrite leads with
- **Quick reference card** at the bottom for day-to-day use

The before/afters use realistic Ottawa/subprime context so they're immediately usable as templates, not just illustrative examples.
SessionEnd hook [_R="${CLAUDE_PLUGIN_ROOT}"; [ -z "$_R" ] && _R="$HOME/.claude/plugins/marketplaces/thedotmack/plugin"; node "$_R/scripts/bun-runner.js" "$_R/scripts/worker-service.cjs" hook claude-code session-complete] failed: Hook cancelled
