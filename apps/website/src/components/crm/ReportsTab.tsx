'use client';
export default function ReportsTab({ tenant }: { tenant: string }): React.ReactElement {
  return <div style={{ padding: '40px', color: '#8888a0', textAlign: 'center' }}>Reports loading...</div>;
}
