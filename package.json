{
  "name": "nexus",
  "version": "0.1.0",
  "private": true,
  "description": "Self-healing multi-agent orchestration framework",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npx vitest run",
    "test:watch": "npx vitest",
    "lint": "npx eslint . --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.80.0",
    "eventemitter3": "^5.0.4",
    "pino": "^10.3.1",
    "zod": "^4.3.6"
  }
}
