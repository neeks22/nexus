Deploy agents for a client. Argument: client name + template name.

Follow the deploy-agents skill: select the template, customize prompts with client context, configure self-healing thresholds, run 10 test interactions, verify alerts work, register in ClientManager with budget, and generate the deployment documentation.

Output: deployment confirmation with health status and next steps.
