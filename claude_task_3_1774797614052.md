Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Here's the file. It's the Ad Copy Generator prompt builder — clean, well-structured. Key pieces:

**What it does:**
- Builds system prompts for Meta/Google ad generation
- Bilingual EN/FR (Quebec French conventions)
- Subprime hooks library (8 hooks per language)
- Full compliance checking for Meta Special Ad Category: Credit

**Exports:**
- `buildAdCopyPrompt(context)` — main prompt builder, assembles 6-7 sections
- `detectComplianceFlag(text)` — returns `COMPLIANT` | `REVIEW` | `FLAGGED`
- `validateCharLimit(text, maxChars)` — platform char limit check
- `validateStyleRules(text)` — ALL CAPS + exclamation mark validation
- Character limit constants (`META_PRIMARY_TEXT_MAX_CHARS`, etc.)

**Structure of generated prompt:**
1. Role (senior automotive copywriter with subprime expertise)
2. Campaign brief (dealership config + vehicle/offer/audience)
3. Output requirements (Meta ads, Google RSAs, retargeting, optional conquest/retention)
4. Hooks library
5. Style rules
6. Compliance rules
7. French conventions (if `fr-CA`)

What do you want to do with it?
SessionEnd hook [_R="${CLAUDE_PLUGIN_ROOT}"; [ -z "$_R" ] && _R="$HOME/.claude/plugins/marketplaces/thedotmack/plugin"; node "$_R/scripts/bun-runner.js" "$_R/scripts/worker-service.cjs" hook claude-code session-complete] failed: Hook cancelled
