Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Here's the current `instant-response.ts` — 527 lines covering:

| Section | What it does |
|---|---|
| `buildInstantResponsePrompt()` | Main assembler — 13 sections |
| `buildRoleSection()` | BDC identity + personality + location |
| `buildCustomerSection()` | First name, touch #, source |
| `buildBilingualProtocol()` | EN/FR detection + Quebec French rules |
| `buildQualificationChecklist()` | HOT/WARM/COLD + 4 fields to gather |
| `buildAppointmentProtocol()` | 5-step booking flow with rep name |
| `buildObjectionHandling()` | 6 objections, EN + FR |
| `buildInventorySection()` | Match list or fallback if empty |
| `buildRequirementsSection()` | Must-have and must-not rules |
| `buildSafetyRailsSection()` | 13 forbidden topics + escalation rules |
| `buildToneSection()` | professional / friendly / casual |
| `buildLanguageSection()` | en-CA or fr-CA directive |
| `buildLengthSection()` | 480 chars SMS, 200 words email |
| `buildBANTOutputSection()` | Internal BANT scoring (1-3 per dimension) |

What do you want to do with it?
SessionEnd hook [_R="${CLAUDE_PLUGIN_ROOT}"; [ -z "$_R" ] && _R="$HOME/.claude/plugins/marketplaces/thedotmack/plugin"; node "$_R/scripts/bun-runner.js" "$_R/scripts/worker-service.cjs" hook claude-code session-complete] failed: Hook cancelled
