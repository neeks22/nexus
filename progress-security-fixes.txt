# Security Fixes — Build Progress

Started: 2026-03-29
PRD: PRD-security-fixes.md

## Build Log

### [2026-03-29 07:46] Epic 1, Item 1: Fix timingSafeEqual in activix-client.ts
- Replaced `===` with `crypto.timingSafeEqual(Buffer.from(...), Buffer.from(...))` in `verifyWebhookSignature`
- Added length check before `timingSafeEqual` to prevent throw on mismatched buffer lengths
- Added test: "rejects signature with different length (timing-safe)"
- All 29 activix-client tests pass

### [2026-03-29 07:50] Epic 1, Item 2: Remove PLACEHOLDER_TOKEN fallback in inbound-reply-handler.json
- No literal `PLACEHOLDER_TOKEN` found — the actual issue was `|| ''` empty-string fallbacks on auth env vars
- Fixed 5 nodes: Lookup Lead, Update CRM Handoff, Send Reply SMS, Send Opt-Out SMS, Update CRM Opt-Out
- Auth tokens ($env.GHL_API_KEY, $env.TWILIO_ACCOUNT_SID, $env.TWILIO_AUTH_TOKEN, $env.ACTIVIX_API_TOKEN) now throw descriptive errors when missing instead of silently using empty strings
- Non-auth fallbacks (message content, name formatting, location ID) left intact — those are safe data defaults
- No tests applicable (n8n workflow JSON file, not TypeScript)

### [2026-03-29 07:55] Epic 1, Item 3: Add Twilio webhook signature validation to inbound-reply-handler
- Added new "Validate Twilio Signature" Code node between "Respond OK" and "Parse Inbound Message"
- Implements full Twilio signature validation: HMAC-SHA1 of (URL + sorted POST params) using TWILIO_AUTH_TOKEN
- Uses timing-safe comparison (crypto.timingSafeEqual) for the signature check
- Skips validation when MESSAGING_PROVIDER is not 'twilio' (e.g., GHL webhooks pass through)
- Requires N8N_WEBHOOK_BASE_URL env var to reconstruct the full webhook URL for validation
- Throws descriptive errors on: missing auth token, missing signature header, missing webhook URL, invalid signature
- No tests applicable (n8n workflow JSON file, not TypeScript)
- JSON validated: 21 nodes, connections correctly wired

### [2026-03-29 07:55] Epic 2, Items 1-3: Dead code cleanup
- Deleted frequency-cap-checker.ts (duplicate of frequency-cap.ts) — zero imports found
- Deleted opt-out-templates.ts (duplicate of templates/opt-out.ts) — zero imports found
- Verified compliance-preflight.ts imports from correct files: frequency-cap.js and opt-out-checker.js
- Verified index.ts exports from correct files: frequency-cap.js, opt-out-checker.js, templates/opt-out.js
- All 920 tests pass after deletion

### [2026-03-29 07:55] Epic 3, Item 1: Add Zod validation on Activix API responses
- All 5 lead methods (get, list, search, create, update) now validate responses against Zod schemas
- Added ActivixValidationError class with detailed issue reporting
- get/create/update validate individual leads via ActivixLeadSchema
- list/search validate paginated responses via PaginatedResponseSchema(ActivixLeadSchema)
- Added 4 tests: invalid get, invalid list, invalid create, valid get
- All 33 activix-client tests pass

### [2026-03-29 07:56] Epic 3, Item 2: Replace module-level let ID counters with crypto.randomUUID()
- lead-transcript.ts: replaced entryCounter with `entry_${randomUUID()}`
- cost-logger.ts: replaced idCounter with `cost_${randomUUID()}`
- trace-logger.ts: replaced traceCounter with `trace-${randomUUID()}`
- All 924 tests pass

### [2026-03-29 07:57] Epic 3, Items 3-5: PII redactor enhancements
- Added street address redaction pattern (number + street name + suffix like St/Ave/Blvd/Dr/Rd/Ln/Ct/Cres etc.)
- Added date-of-birth redaction for MM/DD/YYYY, MM-DD-YYYY, YYYY-MM-DD, YYYY/MM/DD formats
- Fixed US zip code false positives: now requires context (near "zip", "postal", or after state abbreviation)
- Added 17 new tests: 6 zip (context-aware), 5 address, 5 DOB, 1 invalid date rejection
- All 65 PII tests pass

### [2026-03-29 08:00] Epic 4, Items 1-3: Compliance hardening
- Item 1: Wired CASL opt-out language into instant-lead-response Send SMS node
  - Auto-appends bilingual "Reply STOP to unsubscribe. {dealer} | {phone}" to every outbound SMS
  - Uses DEALERSHIP_NAME and DEALERSHIP_PHONE env vars, locale-aware (en-CA/fr-CA)
- Item 2: FrequencyCapChecker now throws if touchHistory is undefined/null
  - Runtime guard prevents silent skip when caller passes undefined from JS
  - Added 2 tests for undefined and null cases
- Item 3: Added ConsentTracker.registerImpliedConsent() convenience method
  - Auto-creates implied consent record with 6-month expiry from consent date
  - Added 5 tests: creation, retrieval, valid within window, expired after window, default date
- All 945 tests pass
