Warning: no stdin data received in 3s, proceeding without it. If piping from a slow command, redirect stdin explicitly: < /dev/null to skip, or wait longer.
Here's the `cold-warming.ts` file — a system prompt builder for cold lead re-engagement. Key structure:

**What it does:**
- Builds a full system prompt for the Cold Warming agent based on `AgentContext`
- Adapts tone by touch number: enthusiastic (1-2) → consultative (3-5) → break-up (6) → monthly nurture (7+)
- Injects inventory matches, conversation history, safety rails, and language directive (EN/FR)

**Sections assembled (in order):**
1. Dealership + rep identity
2. Tone directive (touch-adaptive)
3. Language directive (Quebec FR vs. Canadian EN)
4. Safety rails (no pricing/financing/credit/guarantees)
5. No-repeat instruction (vary angle every touch)
6. Inventory section (up to 3 features per vehicle)
7. Conversation history (numbered, labeled AI vs. Customer)

**Notable:** The break-up message at touch 6 explicitly uses loss aversion framing. Touch 7+ shifts to nurture-only with no CTA.

What do you want to do with it?
SessionEnd hook [_R="${CLAUDE_PLUGIN_ROOT}"; [ -z "$_R" ] && _R="$HOME/.claude/plugins/marketplaces/thedotmack/plugin"; node "$_R/scripts/bun-runner.js" "$_R/scripts/worker-service.cjs" hook claude-code session-complete] failed: Hook cancelled
